╔═══════════════════════════════════════════════════════════════════════════╗
║                                                                           ║
║           📱 INSTALAR AIGenda SaaS NO EASY PANEL - GUIA RÁPIDO            ║
║                                                                           ║
╚═══════════════════════════════════════════════════════════════════════════╝

🎯 O QUE INSTALAR:

┌─────────────────────────────────────────────────────────────────────────┐
│ COMPONENTE          │ VERSÃO   │ PARA QUÊ                               │
├─────────────────────────────────────────────────────────────────────────┤
│ Node.js             │ 20 LTS   │ Executar aplicação (Next.js + Express) │
│ PostgreSQL          │ 16       │ Banco de dados                         │
│ Redis               │ 7        │ Cache e sessões                        │
│ Nginx               │ Latest   │ Reverse proxy + HTTPS                  │
│ PM2                 │ Latest   │ Gerenciar processos 24/7               │
│ Let's Encrypt       │ Latest   │ Certificado SSL gratuito               │
│ Git                 │ Latest   │ Clonar repositório GitHub              │
└─────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════

📋 PASSO-A-PASSO RÁPIDO:

1️⃣ PREPARAR GITHUB
   ├─ git init
   ├─ git add .
   ├─ git commit -m "Initial commit"
   ├─ git remote add origin https://github.com/seu-usuario/AIGenda-SaaS.git
   └─ git push -u origin main

2️⃣ EASY PANEL - CRIAR APLICAÇÃO
   ├─ Criar novo servidor (ou usar existente)
   ├─ Acessar via SSH
   └─ Executar script de instalação

3️⃣ INSTALAR DEPENDÊNCIAS
   ├─ Node.js LTS
   ├─ PostgreSQL 16
   ├─ Redis 7
   ├─ Nginx
   ├─ PM2
   └─ Git

4️⃣ CLONAR REPOSITÓRIO
   └─ git clone https://github.com/seu-usuario/AIGenda-SaaS.git /var/www/aigenda-saas

5️⃣ CONFIGURAR AMBIENTE
   ├─ apps/api/.env
   ├─ apps/web/.env
   └─ Banco de dados PostgreSQL

6️⃣ INSTALAR DEPENDÊNCIAS NPM
   └─ npm install (na raiz)

7️⃣ MIGRAÇÕES DO BANCO
   └─ npm run migrate --workspace=apps/api

8️⃣ BUILD FRONTEND + BACKEND
   └─ npm run build (ambos)

9️⃣ INICIAR COM PM2
   └─ pm2 start ecosystem.config.js

🔟 CONFIGURAR NGINX + SSL
   ├─ Copiar nginx.conf para /etc/nginx/sites-available/aigenda
   ├─ Habilitar site
   ├─ Gerar SSL com Let's Encrypt
   └─ Recarregar Nginx

═══════════════════════════════════════════════════════════════════════════

⚡ COMANDO ÚNICO DE INSTALAÇÃO:

ssh root@seu-ip-do-servidor

apt-get update && apt-get upgrade -y

# Copiar e executar
curl -O https://raw.githubusercontent.com/seu-usuario/AIGenda-SaaS/main/install-easy-panel.sh
chmod +x install-easy-panel.sh
./install-easy-panel.sh

# Clonar e configurar
mkdir -p /var/www/aigenda-saas
cd /var/www/aigenda-saas
git clone https://github.com/seu-usuario/AIGenda-SaaS.git .

# Variáveis de ambiente
cp apps/api/.env.example apps/api/.env
cp apps/web/.env.example apps/web/.env

# Editar .env
nano apps/api/.env
# Alterar: DATABASE_URL, JWT_SECRET, CORS_ORIGIN

# Instalar e build
npm install
npm run migrate --workspace=apps/api
npm run build --workspace=apps/web

# Iniciar
pm2 start ecosystem.config.js
pm2 save

# SSL
certbot certonly --nginx -d seu-dominio.com -d www.seu-dominio.com -d api.seu-dominio.com

# Nginx
cp nginx.conf /etc/nginx/sites-available/aigenda
ln -s /etc/nginx/sites-available/aigenda /etc/nginx/sites-enabled/
systemctl reload nginx

echo "✅ Pronto!"

═══════════════════════════════════════════════════════════════════════════

📁 ESTRUTURA FINAL NO SERVIDOR:

/var/www/aigenda-saas/
├── apps/
│   ├── api/              ← Backend (Express)
│   └── web/              ← Frontend (Next.js)
├── packages/shared/      ← Tipos compartilhados
├── ecosystem.config.js   ← Configuração PM2
├── .env                  ← Variáveis (gitignored)
└── logs/                 ← Logs PM2
    ├── api.log
    ├── api-error.log
    ├── web.log
    └── web-error.log

/etc/nginx/sites-available/
└── aigenda              ← Configuração Nginx

/etc/letsencrypt/live/seu-dominio.com/
├── fullchain.pem        ← Certificado SSL
└── privkey.pem          ← Chave privada

/var/lib/postgresql/
└── 16/                  ← Dados PostgreSQL

═══════════════════════════════════════════════════════════════════════════

🔐 ARQUIVOS NECESSÁRIOS NO PROJETO:

✅ Já criados para você:
  • DEPLOY.md              - Guia completo de deploy
  • EASY-PANEL.md          - Detalhes de instalação
  • install-easy-panel.sh  - Script de instalação
  • ecosystem.config.js    - Configuração PM2
  • nginx.conf             - Configuração Nginx

═══════════════════════════════════════════════════════════════════════════

📊 ARQUITETURA FINAL:

                     Internet
                         ↓
            Nginx (Reverse Proxy) - HTTPS
                    ↙         ↘
            Frontend         Backend
            Next.js          Express
            :3000            :3001
                ↓                ↓
            PM2 (web)      PM2 (api, 2 inst)
                ↓                ↓
            React          Node.js
                                ↓
                    ┌───────────┴────────────┐
                    ↓                        ↓
              PostgreSQL                   Redis
              :5432                        :6379

═══════════════════════════════════════════════════════════════════════════

🔗 DOMÍNIOS:

seu-dominio.com          → Frontend (Next.js)
www.seu-dominio.com      → Frontend (Next.js)
api.seu-dominio.com      → Backend (Express)

═══════════════════════════════════════════════════════════════════════════

💾 VARIÁVEIS DE AMBIENTE NECESSÁRIAS:

Backend (apps/api/.env):
  DATABASE_URL="postgresql://aigenda_user:senha@localhost:5432/aigenda_prod"
  JWT_SECRET="gerar-chave-aleatoria-super-segura"
  NODE_ENV="production"
  PORT=3001
  CORS_ORIGIN="https://seu-dominio.com"

Frontend (apps/web/.env):
  NEXT_PUBLIC_API_URL="https://api.seu-dominio.com"

═══════════════════════════════════════════════════════════════════════════

✅ CHECKLIST PRÉ-DEPLOY:

Repositório GitHub:
  ☐ Criar repositório público/privado
  ☐ Push do código completo
  ☐ .gitignore configurado

Servidor Easy Panel:
  ☐ Node.js 20 LTS instalado
  ☐ PostgreSQL 16 instalado
  ☐ Redis 7 instalado
  ☐ Nginx instalado
  ☐ PM2 instalado globalmente
  ☐ SSH key configurada (se usando SSH)

Aplicação:
  ☐ .env configurado no Backend
  ☐ .env configurado no Frontend
  ☐ Banco de dados criado
  ☐ Migrações executadas
  ☐ Build realizado (npm run build)

Networking:
  ☐ Domínio apontando para servidor (DNS)
  ☐ Firewall permitindo portas 80, 443
  ☐ PM2 iniciado (pm2 start ecosystem.config.js)
  ☐ Nginx configurado e recarregado
  ☐ Certificado SSL gerado

Testing:
  ☐ Frontend acessível em seu-dominio.com
  ☐ API acessível em api.seu-dominio.com/health
  ☐ HTTPS funcionando (sem avisos)
  ☐ Login funcionando

═══════════════════════════════════════════════════════════════════════════

📞 SUPORTE & TROUBLESHOOTING:

Ver logs:
  pm2 logs aigenda-api
  pm2 logs aigenda-web
  tail -f /var/log/nginx/aigenda-web-access.log

Reiniciar aplicações:
  pm2 restart all
  pm2 restart aigenda-api
  pm2 restart aigenda-web

Status:
  pm2 status
  pm2 monit

Recarregar Nginx:
  systemctl reload nginx
  nginx -t (testar configuração)

Certificado SSL:
  certbot renew
  certbot renew --dry-run (teste)

═══════════════════════════════════════════════════════════════════════════

📚 DOCUMENTAÇÃO CRIADA:

1. DEPLOY.md          ← Guia detalhado (14 passos)
2. EASY-PANEL.md      ← Foco em Easy Panel
3. install-easy-panel.sh ← Script automático
4. ecosystem.config.js   ← PM2 configuration
5. nginx.conf            ← Nginx configuration

═══════════════════════════════════════════════════════════════════════════

🎉 PRÓXIMAS ETAPAS:

1. Preparar repositório GitHub
2. Executar script de instalação
3. Seguir documentação (DEPLOY.md)
4. Configurar domínios no DNS
5. Testar tudo
6. Monitorar logs

═══════════════════════════════════════════════════════════════════════════

Precisa de ajuda? Veja os arquivos:

📖 Guia completo:     DEPLOY.md
📖 Especifico EP:     EASY-PANEL.md
📖 O que instalar:    Este arquivo

BOA SORTE! 🚀

═══════════════════════════════════════════════════════════════════════════
